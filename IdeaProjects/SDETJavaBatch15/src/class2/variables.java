package class2;

public class variables {
    public static void main(String[] args) {
        /*
        Bellow line is reserving a box in the computers memory.
        we are calling it box1 if we need the information that we are storing inside this box
        we can simple say to the computer give us the contents of box1
        in=> what type pf data we want to store
        box=1=> is the name of the box
        = sign will take whatever we write after it and will store it inside the box1
        10=> si what we are storing inside the box
         */
        int box1=10;
        // if we are printing something from a box we don't need""
        System.out.println(box1); // bring the contents of box1 and print them on the console
        /*To store whole numbers ww have 4 different types of boxes.
         for example like 10 20 3000 100000 33000
         1] byte 2] short3] int 4] long
         */
        byte box2=127; // range for byte is from -128 to 127 if we need to store larger numbers we should try short
        byte box3=-128;
        short box4=32767;
        short box5=3256;
        int biggerBox=123456684; // most common type of box
        long maxBox=1523434534564l;
        /*
        Reserve a box which can hold the number 10000 call it and print its value on the console
         */
        // Break till 12:00 EST

        short myBox=10000;
        System.out.println(myBox);





    }

}
